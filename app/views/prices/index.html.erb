<p id="notice"><%= notice %></p>

<h1>Listing Prices</h1>

<table>
  <thead>
    <tr>
      <th>Date</th>
      <th>Price</th>
      <th>Memo</th>
      <th>Familyuser</th>
      <th>Payment</th>
      <th>Loginuser</th>
      <th>Type</th>
      <th>Category</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @prices.each do |price| %>
      <tr>
        <td><%= price.date %></td>
        <td><%= price.price %></td>
        <td><%= price.memo %></td>
        <td><%= price.familyuser_id %></td>
        <td><%= price.payment %></td>
        <td><%= price.loginuser_id %></td>
        <td><%= price.type %></td>
        <td><%= price.category_id %></td>
        <td><%= link_to 'Show', price %></td>
        <td><%= link_to 'Edit', edit_price_path(price) %></td>
        <td><%= link_to 'Destroy', price, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Price', new_price_path %>
