<p id="notice"><%= notice %></p>

<h1>収入履歴</h1>

<table>

  <tbody>
    <% @incomehistories.each do |price| %>
      <tr>
        <td><%= link_to "#{price.date} 　　￥#{price.price}", price %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
