<script type="text/javascript">
    $(function () {
      $('.demo001').pickadate();
    });
</script>


<%= form_for(@notice) do |f| %>
  <% if @notice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@notice.errors.count, "error") %> prohibited this notice from being saved:</h2>

      <ul>
      <% @notice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :日付 %><br>
    <%= f.text_field :date, :class => "demo001", :placeholder => "クリックしてください。"%>
  </div>
  <div class="field">
    <%= f.label :タイトル %><br>
    <%= f.text_field :title, :placeholder => "※必ず入力してください"%>
  </div>
  <div class="field">
    <%= f.label :メモ %><br>
    <%= f.text_field :memo, :placeholder => "※任意で入力してください" %>
  </div>

  <div class="actions">
    <%= f.submit "更新" %>
  </div>
<% end %>
